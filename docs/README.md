# Demo

This directory contains an interactive demo of the `@zachcp/molstar-components` package.  This shows use in HTMl using import maps. Components can also be used as standats TSX components within javascript projects.


```bash
# Using Python
python -m http.server 8000
```
